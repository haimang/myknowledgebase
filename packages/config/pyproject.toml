[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "smind-config"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["pydantic-settings>=2.3.0"]

[tool.setuptools.packages.find]
where = ["src"]
