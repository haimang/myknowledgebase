[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "smind-rag-vectorizer"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["smind-vector-sqlite-vec"]

[tool.setuptools.packages.find]
where = ["src"]

