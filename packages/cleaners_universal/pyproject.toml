[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "smind-cleaners-universal"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["smind-browser-runtime"]

[tool.setuptools.packages.find]
where = ["src"]

